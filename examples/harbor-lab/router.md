# Harbor Lab router

Compact. Not a source of truth.

## Recovery

Operator correction outranks every row. Current file state outranks memory. Open the smallest owner. Do not load the pack.

## Where to go

| If the task is... | Open |
| --- | --- |
| What does this word mean in the shop? | concepts.csv |
| What is standing policy? | rules.csv |
| A messy new note | sources.csv then intake_log.csv |
| Did a write actually land? | intake_log.csv |
| I forgot which file holds X | file_index.csv |
| I am lost about the whole shop | system_map.csv then stop |

## Natural commands

- "file this" → sources.csv, then route, then read back
- "is this policy" → rules.csv only
- "what do we think caused the fence drift" → concepts.csv row C-001 and C-004. Do not invent a third home.
- "you slipped" → re-read this router and the single owner for the live task

## Do not

- Recite every row into a prompt
- Mark VERIFIED without read-back
- Promote SRC-003 to a rule
